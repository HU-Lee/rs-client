// Dark Mode, Light Mode

const darkMode = {
    titleColor : 'white',
    bgColor: 'black',
}

const lightMode ={
    titleColor : 'black',
    bgColor: 'white',
}

export const theme = {
    lightMode,
    darkMode
}
